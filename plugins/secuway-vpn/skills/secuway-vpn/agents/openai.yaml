interface:
  display_name: "Secuway VPN"
  short_description: "Cross-platform Secuway VPN enrollment and connection"
  default_prompt: "Use $secuway-vpn to diagnose this host, perform one-time interactive enrollment, and connect without moving credentials between hosts."
